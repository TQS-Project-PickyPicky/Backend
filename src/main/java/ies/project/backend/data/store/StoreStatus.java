package ies.project.backend.data.store;

public enum StoreStatus {
    ACCEPTED, // Store accepted the order
    REJECTED, // Store rejected the order
    PENDING // Store has not yet accepted or rejected the order
}

